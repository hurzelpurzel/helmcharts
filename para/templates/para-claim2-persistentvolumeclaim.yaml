apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: para-claim2
  name: para-claim2
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
